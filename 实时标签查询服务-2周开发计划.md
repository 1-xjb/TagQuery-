# 实时标签查询服务 — 2周详细开发计划（独立开发版）

> 基于 `标签实时查询需求.pdf` PRD v0.3（2026-06-22）
> 适用场景：单人独立开发，从零搭建

---

## 一、项目目标

### 1.1 产品定位

建设一套统一的实时标签查询后端服务，提供标准 API 接口能力。无前端页面，纯服务端。

### 1.2 核心交付物

| # | 模块 | 一句话目标 | 优先级 |
|---|------|------------|--------|
| 1 | **实时标签查询** | 外部调用方传入加密手机号 → 返回命中的标签 Code 列表 | P0 |
| 2 | **数据拉取与入库** | 平台定时从客户 S3 拉取离线文件 → 解析 → 全量灌入 Redis | P0 |
| 3 | **日志与监控** | 9 类日志全覆盖 + 8 项 Prometheus 监控指标 + Grafana 大盘 | P0 |
| 4 | **数据版本管理** | 每次入库生成独立版本，可切换生效、异常可一键回退 | P1 |

### 1.3 关键非功能指标

| 指标 | 要求 |
|------|------|
| QPS | 1000（客户维度） |
| 响应延迟 | P99 ≤ 1000ms |
| 数据规模 | 十亿级 ID |
| 批量上限 | 待压测确认，默认 1000 |
| 数据隔离 | 不同客户不同 Redis 前缀/实例 |
| 可用性 | 版本异常可回退，查询不中断 |

---

## 二、项目目录结构

```
tag-query-service/
├── pom.xml                          # Maven 工程（以 Java/Spring Boot 为例）
├── src/
│   ├── main/
│   │   ├── java/com/platform/tagquery/
│   │   │   ├── TagQueryApplication.java              # 启动类
│   │   │   │
│   │   │   ├── config/                               # 配置类
│   │   │   │   ├── RedisConfig.java                  # Redis 连接池配置
│   │   │   │   ├── S3Config.java                     # S3 客户端配置
│   │   │   │   ├── ThreadPoolConfig.java             # 线程池配置（拉取/入库/邮件）
│   │   │   │   └── RateLimiterConfig.java            # 限流器配置
│   │   │   │
│   │   │   ├── controller/                           # 对外 API 层
│   │   │   │   ├── TagQueryController.java           # 标签查询 API
│   │   │   │   ├── PullConfigController.java         # 拉取配置管理 API
│   │   │   │   ├── PullTaskController.java           # 拉取任务触发/查询 API
│   │   │   │   ├── VersionController.java            # 版本管理 API
│   │   │   │   ├── AppKeyController.java             # AppKey 管理 API
│   │   │   │   └── HealthController.java             # 健康检查 API
│   │   │   │
│   │   │   ├── service/                              # 业务逻辑层
│   │   │   │   ├── TagQueryService.java              # 核心：标签查询逻辑
│   │   │   │   ├── AuthService.java                  # 鉴权 & 授权
│   │   │   │   ├── RateLimitService.java             # 限流控制
│   │   │   │   ├── PullConfigService.java            # S3 拉取配置管理
│   │   │   │   ├── PullTaskService.java              # 拉取任务执行
│   │   │   │   ├── DataIntegrationService.java       # 数据解析 & 入库
│   │   │   │   ├── VersionService.java               # 版本管理
│   │   │   │   ├── LogService.java                   # 日志记录
│   │   │   │   └── NotificationService.java          # 邮件通知
│   │   │   │
│   │   │   ├── repository/                           # 数据访问层
│   │   │   │   ├── redis/
│   │   │   │   │   └── TagRedisRepository.java       # Redis 标签读写
│   │   │   │   └── mysql/
│   │   │   │       ├── AppKeyRepository.java
│   │   │   │       ├── PullConfigRepository.java
│   │   │   │       ├── PullTaskRepository.java
│   │   │   │       ├── DataVersionRepository.java
│   │   │   │       └── LogRepositories.java
│   │   │   │
│   │   │   ├── model/                                # 数据模型
│   │   │   │   ├── entity/                           # DB 实体
│   │   │   │   ├── dto/                              # 请求/响应 DTO
│   │   │   │   ├── enums/                            # 枚举
│   │   │   │   └── vo/                               # 值对象
│   │   │   │
│   │   │   ├── middleware/                            # 拦截器/过滤器
│   │   │   │   ├── AuthInterceptor.java              # AppKey+签名+时间戳校验
│   │   │   │   ├── RateLimitInterceptor.java         # QPS 限流
│   │   │   │   └── RequestIdInterceptor.java         # requestId 生成与透传
│   │   │   │
│   │   │   ├── scheduler/                            # 定时任务
│   │   │   │   └── PullTaskScheduler.java            # 按 cron 触发 S3 拉取
│   │   │   │
│   │   │   ├── integration/                          # 外部集成
│   │   │   │   ├── S3Client.java                     # S3 文件操作封装
│   │   │   │   └── EmailClient.java                  # 邮件发送封装
│   │   │   │
│   │   │   ├── util/                                 # 工具类
│   │   │   │   ├── SignatureUtil.java                # 签名生成/校验
│   │   │   │   ├── DataParser.java                   # 标签文件格式解析
│   │   │   │   ├── IdEncoder.java                    # 手机号加密/脱敏
│   │   │   │   └── RequestIdGenerator.java           # requestId 生成
│   │   │   │
│   │   │   └── exception/                            # 异常 & 错误码
│   │   │       ├── ErrorCode.java                    # 14 种错误码枚举
│   │   │       ├── BizException.java                 # 业务异常
│   │   │       └── GlobalExceptionHandler.java       # 全局异常处理
│   │   │
│   │   └── resources/
│   │       ├── application.yml                       # 主配置
│   │       ├── application-dev.yml                   # 开发环境
│   │       ├── application-prod.yml                  # 生产环境
│   │       ├── db/migration/                         # Flyway 迁移脚本
│   │       │   ├── V1__init_schema.sql
│   │       │   └── V2__seed_data.sql
│   │       └── templates/                            # 邮件模板
│   │           ├── pull-failure-alert.html
│   │           ├── integration-failure-alert.html
│   │           └── task-complete-notification.html
│   │
│   └── test/
│       └── java/com/platform/tagquery/
│           ├── unit/                                 # 单元测试
│           │   ├── TagQueryServiceTest.java
│           │   ├── AuthServiceTest.java
│           │   └── DataParserTest.java
│           └── integration/                          # 集成测试
│               └── TagQueryIntegrationTest.java
│
├── deploy/
│   ├── Dockerfile
│   └── docker-compose.yml                            # Redis + MySQL + 应用
├── scripts/
│   ├── init-redis.sh                                 # Redis 初始化脚本
│   └── load-test/                                    # 压测脚本
│       └── locustfile.py
└── docs/
    ├── api-spec.yaml                                 # OpenAPI 3.0 接口文档
    ├── redis-design.md                               # Redis 数据结构设计
    ├── error-codes.md                                # 错误码说明
    └── deploy-guide.md                               # 部署文档
```

---

## 三、API 接口清单（共 16 个）

### 3.1 对外接口：实时标签查询

#### `POST /api/v1/tag/query`

**用途**：调用方批量查询标签 Code

**请求示例**：
```json
{
  "appKey": "customer_a_app",
  "signature": "a1b2c3...",
  "timestamp": 1750932000000,
  "dataSourceId": "ds_lead_scoring_001",
  "ids": [
    "e10adc3949ba59abbe56e057f20f883e",
    "5d41402abc4b2a76b9719d911017c592"
  ]
}
```

**响应示例（成功）**：
```json
{
  "code": 0,
  "message": "success",
  "requestId": "req_20260725_a1b2c3d4",
  "versionId": "v_ds_lead_scoring_001_20260725_01",
  "data": [
    {
      "id": "e10adc3949ba59abbe56e057f20f883e",
      "tags": ["TAG_A001", "TAG_B003", "TAG_C007"]
    },
    {
      "id": "5d41402abc4b2a76b9719d911017c592",
      "tags": []
    }
  ],
  "stats": {
    "totalIds": 2,
    "hitIds": 1,
    "hitRate": 0.5,
    "costMs": 15
  }
}
```

**响应示例（鉴权失败）**：
```json
{
  "code": 10001,
  "message": "调用身份无效，请确认 AppKey 是否正确或是否已启用",
  "requestId": "req_20260725_a1b2c3d4"
}
```

---

### 3.2 管理接口：AppKey 管理

| 方法 | 路径 | 用途 |
|------|------|------|
| `POST` | `/api/v1/admin/app-keys` | 注册新 AppKey |
| `GET` | `/api/v1/admin/app-keys` | 分页查询 AppKey 列表 |
| `GET` | `/api/v1/admin/app-keys/{appKeyId}` | 查询单个 AppKey 详情 |
| `PUT` | `/api/v1/admin/app-keys/{appKeyId}` | 更新 AppKey 信息 |
| `PATCH` | `/api/v1/admin/app-keys/{appKeyId}/status` | 启用/停用 AppKey |
| `POST` | `/api/v1/admin/app-keys/{appKeyId}/grant` | 授权数据源给 AppKey |

### 3.3 管理接口：拉取配置

| 方法 | 路径 | 用途 |
|------|------|------|
| `POST` | `/api/v1/admin/pull-configs` | 创建拉取配置 |
| `GET` | `/api/v1/admin/pull-configs` | 分页查询拉取配置列表 |
| `GET` | `/api/v1/admin/pull-configs/{configId}` | 查询单个拉取配置详情 |
| `PUT` | `/api/v1/admin/pull-configs/{configId}` | 更新拉取配置 |
| `PATCH` | `/api/v1/admin/pull-configs/{configId}/status` | 启用/停用拉取配置 |

### 3.4 管理接口：拉取任务

| 方法 | 路径 | 用途 |
|------|------|------|
| `POST` | `/api/v1/admin/pull-tasks` | 手动触发拉取任务 |
| `GET` | `/api/v1/admin/pull-tasks/{taskId}` | 查询拉取任务状态 |
| `GET` | `/api/v1/admin/pull-tasks` | 分页查询拉取任务历史 |

### 3.5 管理接口：版本管理

| 方法 | 路径 | 用途 |
|------|------|------|
| `GET` | `/api/v1/admin/versions` | 分页查询版本列表 |
| `POST` | `/api/v1/admin/versions/{versionId}/activate` | 激活（切换生效）指定版本 |
| `POST` | `/api/v1/admin/versions/{versionId}/rollback` | 回退到指定版本 |

### 3.6 运维接口

| 方法 | 路径 | 用途 |
|------|------|------|
| `GET` | `/api/v1/health` | 健康检查（Redis/MySQL/S3 连通性） |
| `GET` | `/actuator/prometheus` | Prometheus 指标暴露（Spring Actuator） |

---

## 四、数据库表设计（MySQL，8 张表）

### 4.1 `app_key` — 调用方身份

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT PK AUTO_INCREMENT | 自增主键 |
| `app_key` | VARCHAR(64) UNIQUE NOT NULL | AppKey 标识 |
| `app_name` | VARCHAR(128) NOT NULL | 调用方名称 |
| `app_secret` | VARCHAR(256) NOT NULL | 签名密钥（加密存储） |
| `status` | TINYINT NOT NULL DEFAULT 1 | 0=停用, 1=启用 |
| `created_at` | DATETIME NOT NULL | 创建时间 |
| `updated_at` | DATETIME NOT NULL | 更新时间 |

### 4.2 `data_source` — 数据源

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT PK AUTO_INCREMENT | 自增主键 |
| `source_id` | VARCHAR(64) UNIQUE NOT NULL | 数据源标识（如 `ds_lead_scoring_001`） |
| `source_name` | VARCHAR(128) NOT NULL | 数据源名称 |
| `customer_name` | VARCHAR(128) NOT NULL | 所属客户 |
| `status` | TINYINT NOT NULL DEFAULT 1 | 0=停用, 1=启用 |
| `created_at` | DATETIME NOT NULL | 创建时间 |

### 4.3 `app_key_data_source` — AppKey ↔ 数据源授权映射

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT PK AUTO_INCREMENT | 自增主键 |
| `app_key_id` | BIGINT NOT NULL FK→app_key.id | AppKey ID |
| `data_source_id` | BIGINT NOT NULL FK→data_source.id | 数据源 ID |
| `created_at` | DATETIME NOT NULL | 授权时间 |
| UNIQUE | (`app_key_id`, `data_source_id`) | 去重 |

### 4.4 `pull_config` — 拉取配置

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT PK AUTO_INCREMENT | 自增主键 |
| `customer_name` | VARCHAR(128) NOT NULL | 客户名称 |
| `data_source_id` | VARCHAR(64) NOT NULL | 数据源标识 |
| `s3_endpoint` | VARCHAR(256) NOT NULL | S3 地址 |
| `s3_bucket` | VARCHAR(128) NOT NULL | S3 Bucket |
| `s3_prefix` | VARCHAR(256) NOT NULL | S3 路径前缀 |
| `cron_expression` | VARCHAR(64) NOT NULL | 拉取周期（cron） |
| `partition_rule` | VARCHAR(32) NOT NULL | 分区规则：LATEST / CURRENT_MONTH |
| `status` | TINYINT NOT NULL DEFAULT 1 | 0=停用, 1=启用 |
| `created_at` | DATETIME NOT NULL | 创建时间 |
| `updated_at` | DATETIME NOT NULL | 更新时间 |

### 4.5 `pull_task` — 拉取任务

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT PK AUTO_INCREMENT | 自增主键 |
| `pull_config_id` | BIGINT NOT NULL FK→pull_config.id | 关联拉取配置 |
| `s3_full_path` | VARCHAR(512) NOT NULL | 实际拉取的完整 S3 路径 |
| `partition` | VARCHAR(128) NOT NULL | 目标分区 |
| `trigger_type` | VARCHAR(16) NOT NULL | 触发方式：SCHEDULED / MANUAL |
| `status` | VARCHAR(32) NOT NULL | 状态：PENDING / PULLING / PULLED / FAILED |
| `file_count` | INT DEFAULT 0 | 拉取文件数 |
| `file_total_size` | BIGINT DEFAULT 0 | 文件总大小 (bytes) |
| `failure_reason` | TEXT | 失败原因 |
| `started_at` | DATETIME | 开始时间 |
| `finished_at` | DATETIME | 结束时间 |
| `created_at` | DATETIME NOT NULL | 创建时间 |

### 4.6 `integration_task` — 数据集成任务

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT PK AUTO_INCREMENT | 自增主键 |
| `pull_task_id` | BIGINT NOT NULL FK→pull_task.id | 关联拉取任务 |
| `data_source_id` | VARCHAR(64) NOT NULL | 数据源标识 |
| `status` | VARCHAR(32) NOT NULL | 状态：PENDING / PARSING / VALIDATING / LOADING / SUCCESS / FAILED |
| `total_records` | BIGINT DEFAULT 0 | 总记录数 |
| `valid_records` | BIGINT DEFAULT 0 | 有效记录数 |
| `duplicate_rate` | DECIMAL(5,4) DEFAULT 0 | 重复率 |
| `null_rate` | DECIMAL(5,4) DEFAULT 0 | 空值率 |
| `failure_reason` | TEXT | 失败原因 |
| `started_at` | DATETIME | 开始时间 |
| `finished_at` | DATETIME | 结束时间 |
| `created_at` | DATETIME NOT NULL | 创建时间 |

### 4.7 `data_version` — 数据版本

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT PK AUTO_INCREMENT | 自增主键 |
| `version_key` | VARCHAR(128) UNIQUE NOT NULL | 版本标识，如 `v_ds_001_20260725_01` |
| `data_source_id` | VARCHAR(64) NOT NULL | 数据源标识 |
| `integration_task_id` | BIGINT NOT NULL FK→integration_task.id | 关联集成任务 |
| `record_count` | BIGINT NOT NULL | 包含记录数 |
| `validate_status` | VARCHAR(32) NOT NULL | 校验状态：PENDING / PASSED / FAILED |
| `active_status` | VARCHAR(32) NOT NULL | 生效状态：INACTIVE / ACTIVE / DEPRECATED |
| `validate_failure_reason` | TEXT | 校验失败原因 |
| `activated_at` | DATETIME | 生效时间 |
| `created_at` | DATETIME NOT NULL | 创建时间 |

### 4.8 `version_event_log` — 版本操作日志

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT PK AUTO_INCREMENT | 自增主键 |
| `data_source_id` | VARCHAR(64) NOT NULL | 数据源标识 |
| `action` | VARCHAR(32) NOT NULL | 操作类型：ACTIVATE / ROLLBACK |
| `from_version_key` | VARCHAR(128) | 操作前版本 |
| `to_version_key` | VARCHAR(128) | 操作后版本 |
| `result` | VARCHAR(32) NOT NULL | 结果：SUCCESS / FAILED |
| `reason` | TEXT | 操作原因/失败原因 |
| `created_at` | DATETIME NOT NULL | 操作时间 |

---

## 五、Redis 数据结构设计

### 5.1 设计原则

- **版本隔离**：Key 前缀包含 versionKey，版本切换 = 改路由前缀
- **客户隔离**：前缀包含 dataSourceId
- **批量查询**：使用 `MGET` 或 `Pipeline`（十亿级建议用 LUA 脚本或专业 KV 方案）

### 5.2 Key 设计

```
# 标签数据（Hash 结构，一个 ID 的标签存储在一个 Set 中）
tag:{dataSourceId}:{versionKey}:{id}  →  Set<String>

# 示例：
tag:ds_lead_scoring_001:v_20260725_01:e10adc3949ba59abbe56e057f20f883e
  → SMEMBERS → ["TAG_A001", "TAG_B003", "TAG_C007"]

# 生效版本指针（String）
tag:active_version:{dataSourceId}  →  String  versionKey

# 示例：
tag:active_version:ds_lead_scoring_001  →  "v_20260725_01"

# 版本 → 包含的所有 ID（Set，用于版本管理/统计）
tag:version_ids:{dataSourceId}:{versionKey}  →  Set<String>

# 版本元信息（Hash）
tag:version_meta:{dataSourceId}:{versionKey}
  →  { recordCount: 1000000, status: "ACTIVE", createdAt: "2026-07-25T10:00:00Z" }
```

### 5.3 查询流程

```
1. 读生效版本: GET tag:active_version:{dataSourceId} → "v_20260725_01"
2. 组装批量查询 Key: ["tag:ds_001:v_20260725_01:id1", "tag:ds_001:v_20260725_01:id2", ...]
3. Pipeline SMEMBERS 所有 Key → 结果映射
4. 组装返回
```

### 5.4 入库流程

```
1. 生成新 versionKey
2. 逐批写入: SADD tag:{ds}:{newVersion}:{id} Code1 Code2 ...
   同时记录: SADD tag:version_ids:{ds}:{newVersion} {id}
3. 校验通过后更新生效版本: SET tag:active_version:{ds} {newVersion}
```

---

## 六、核心模块函数签名设计

### 6.1 AuthService — 鉴权 & 授权

```java
package com.platform.tagquery.service;

public interface AuthService {

    /**
     * 校验 AppKey 是否存在且启用
     * @param appKey 调用方标识
     * @return AppKeyInfo（含 appKeyId、appName、status、appSecret）
     * @throws BizException(ErrorCode.APP_KEY_INVALID) AppKey 不存在或已停用
     */
    AppKeyInfo validateAppKey(String appKey);

    /**
     * 校验请求签名（HMAC-SHA256）
     * 签名规则: HMAC-SHA256(appKey + timestamp + ids.join("|") + dataSourceId, appSecret)
     * @param appKey    调用方标识
     * @param timestamp 请求时间戳
     * @param ids       查询 ID 列表（参与签名）
     * @param signature 调用方传入的签名
     * @param appSecret AppKey 对应的密钥
     * @return true=校验通过
     * @throws BizException(ErrorCode.SIGNATURE_INVALID) 签名校验失败
     */
    boolean validateSignature(String appKey, long timestamp,
                              List<String> ids, String dataSourceId,
                              String signature, String appSecret);

    /**
     * 校验时间戳是否在有效期内（防重放攻击）
     * @param timestamp  请求时间戳（毫秒）
     * @param ttlSeconds 允许的时间偏差（默认 300 秒）
     * @return true=有效
     * @throws BizException(ErrorCode.TIMESTAMP_EXPIRED) 时间戳过期
     */
    boolean validateTimestamp(long timestamp, int ttlSeconds);

    /**
     * 校验 AppKey 是否有权限访问指定数据源
     * @param appKey       调用方标识
     * @param dataSourceId 数据源标识
     * @return true=有权限
     * @throws BizException(ErrorCode.DATA_SOURCE_UNAUTHORIZED) 无权限
     */
    boolean checkDataSourceAuth(String appKey, String dataSourceId);
}
```

### 6.2 RateLimitService — 限流

```java
package com.platform.tagquery.service;

public interface RateLimitService {

    /**
     * 尝试获取一个查询许可（滑动窗口限流）
     * @param appKey       调用方标识
     * @param dataSourceId 数据源标识
     * @return true=允许通过
     * @throws BizException(ErrorCode.RATE_LIMITED) 触发限流
     */
    boolean tryAcquire(String appKey, String dataSourceId);

    /**
     * 获取指定 AppKey 当前秒内请求计数（供监控读取）
     * @param appKey 调用方标识
     * @return 当前秒内请求数
     */
    long getCurrentQps(String appKey);

    /**
     * 获取 AppKey 配置的 QPS 上限
     * @param appKey 调用方标识
     * @return QPS 上限值
     */
    int getMaxQps(String appKey);
}
```

### 6.3 TagQueryService — 核心查询

```java
package com.platform.tagquery.service;

public interface TagQueryService {

    /**
     * 批量查询标签 Code（对外调用的统一入口）
     * 内部流程：获取生效版本 → 批量查 Redis → 统计命中率 → 组装响应
     * @param request 查询请求
     * @return 查询结果
     */
    TagQueryResponse queryTags(TagQueryRequest request);

    /**
     * 内部：从 Redis 批量读取标签数据
     * @param dataSourceId 数据源标识
     * @param versionKey   当前生效版本
     * @param ids          用户标识列表
     * @return Map<id, List<tagCode>>
     */
    Map<String, List<String>> batchReadFromRedis(
            String dataSourceId, String versionKey, List<String> ids);

    /**
     * 内部：从 Redis 获取当前生效版本
     * @param dataSourceId 数据源标识
     * @return 版本标识
     * @throws BizException(ErrorCode.REDIS_UNAVAILABLE) Redis 不可用
     */
    String getActiveVersion(String dataSourceId);
}
```

### 6.4 TagRedisRepository — Redis 标签操作（关键实现类）

```java
package com.platform.tagquery.repository.redis;

@Component
public class TagRedisRepository {

    private final StringRedisTemplate redisTemplate;

    /**
     * 批量查询标签（Pipeline 方式，避免 N 次网络往返）
     * @param dataSourceId 数据源标识
     * @param versionKey   版本标识
     * @param ids          用户标识列表
     * @return Map<id, Set<tagCode>>
     */
    public Map<String, Set<String>> batchGetTags(
            String dataSourceId, String versionKey, List<String> ids);

    /**
     * 全量灌入一个版本的标签数据（Pipeline 分批写入）
     * @param dataSourceId 数据源标识
     * @param versionKey   版本标识
     * @param batch        一批 ID→Tags 映射
     */
    public void batchPutTags(String dataSourceId, String versionKey,
                             Map<String, List<String>> batch);

    /**
     * 记录某版本包含的所有 ID（用于版本管理和统计）
     * @param dataSourceId 数据源标识
     * @param versionKey   版本标识
     * @param ids          ID 集合
     */
    public void putVersionIds(String dataSourceId, String versionKey,
                              Set<String> ids);

    /**
     * 设置生效版本（原子操作 SET）
     * @param dataSourceId  数据源标识
     * @param newVersionKey 新版本标识
     */
    public void setActiveVersion(String dataSourceId, String newVersionKey);

    /**
     * 获取当前生效版本
     * @param dataSourceId 数据源标识
     * @return 版本标识，不存在返回 null
     */
    public String getActiveVersion(String dataSourceId);

    /**
     * 设置版本元信息（HSET）
     * @param dataSourceId 数据源标识
     * @param versionKey   版本标识
     * @param meta         元信息 Map（recordCount, status, createdAt...）
     */
    public void putVersionMeta(String dataSourceId, String versionKey,
                               Map<String, String> meta);

    /**
     * 获取版本元信息（HGETALL）
     */
    public Map<String, String> getVersionMeta(String dataSourceId,
                                               String versionKey);

    /**
     * 删除指定版本的全部数据（清理旧版本时使用）
     * @param dataSourceId 数据源标识
     * @param versionKey   版本标识
     */
    public void deleteVersion(String dataSourceId, String versionKey);

    /**
     * Ping 检查 Redis 连通性
     * @return true=可用
     */
    public boolean ping();
}
```

### 6.5 PullConfigService — 拉取配置管理

```java
package com.platform.tagquery.service;

public interface PullConfigService {

    /** 创建拉取配置 */
    PullConfigDTO createPullConfig(CreatePullConfigRequest dto);

    /** 更新拉取配置 */
    PullConfigDTO updatePullConfig(Long configId, UpdatePullConfigRequest dto);

    /** 查询单个拉取配置 */
    PullConfigDTO getPullConfig(Long configId);

    /** 分页查询拉取配置列表 */
    PageResult<PullConfigDTO> listPullConfigs(String customerName,
            String dataSourceId, Integer status, int page, int size);

    /** 启用/停用拉取配置 */
    void toggleConfigStatus(Long configId, boolean enable);

    /** 查询所有已启用且到达拉取时间的配置（供调度器调用） */
    List<PullConfigDTO> getDuePullConfigs();
}
```

### 6.6 PullTaskService — 拉取任务执行

```java
package com.platform.tagquery.service;

public interface PullTaskService {

    /**
     * 手动触发拉取任务
     * @param configId  拉取配置 ID
     * @param partition 指定分区（可选，不传则按配置规则自动确定）
     * @return 拉取任务
     */
    PullTaskDTO triggerManualPull(Long configId, String partition);

    /**
     * 定时任务触发拉取（供 PullTaskScheduler 调用）
     * @param config 拉取配置
     * @return 拉取任务，配置未到期返回 null
     */
    PullTaskDTO triggerScheduledPull(PullConfigDTO config);

    /** 查询任务状态 */
    PullTaskDTO getTaskStatus(Long taskId);

    /** 分页查询任务历史 */
    PageResult<PullTaskDTO> listPullTasks(Long configId, String status,
                                           int page, int size);

    // === 内部方法 ===

    /** 解析 S3 分区路径 */
    String resolvePartition(String s3Prefix, String rule);

    /** 从 S3 下载文件到本地临时目录 */
    List<DownloadedFile> downloadFiles(String s3FullPath, String localDir);

    /** 更新任务状态 */
    void updateTaskStatus(Long taskId, PullTaskStatus status,
                          String failureReason);
}
```

### 6.7 DataIntegrationService — 数据解析 & 入库

```java
package com.platform.tagquery.service;

public interface DataIntegrationService {

    /**
     * 执行数据集成（拉取成功后自动调用）
     * @param pullTaskId 拉取任务 ID
     * @return 集成任务结果
     */
    IntegrationTaskDTO runIntegration(Long pullTaskId);

    /**
     * 解析标签数据文件（流式，逐行读取，避免大文件 OOM）
     * @param filePath 本地文件路径
     * @return 解析后的数据迭代器
     */
    Iterator<ParsedRecord> parseDataFile(String filePath);

    /**
     * 数据校验（统计总行数、重复率、空值率、格式错误率）
     * @param records 数据迭代器
     * @return 校验报告
     */
    ValidationReport validateData(Iterator<ParsedRecord> records);

    /**
     * 将校验通过的数据灌入 Redis（全量替换，分批 Pipeline）
     * @param dataSourceId 数据源标识
     * @param versionKey   目标版本
     * @param records      数据迭代器
     * @param batchSize    每批写入条数（默认 1000）
     * @return 入库统计
     */
    LoadResult loadToRedis(String dataSourceId, String versionKey,
                           Iterator<ParsedRecord> records, int batchSize);
}

// === 数据模型 ===

/** 解析后的单条记录 */
class ParsedRecord {
    String userId;           // 第 1 段：用户标识
    String idType;           // 第 2 段：标识类型
    List<String> tagCodes;   // 第 3 段及以后：标签 Code 列表
    int lineNumber;          // 源文件行号
}

/** 校验报告 */
class ValidationReport {
    long totalRecords;
    long validRecords;
    long duplicateRecords;
    long nullUserIdRecords;
    long emptyTagRecords;
    long invalidFormatRecords;
    double duplicateRate;     // > 阈值则校验失败
    double nullRate;          // > 阈值则校验失败
    boolean passed;
    List<String> errors;
}

/** 入库结果 */
class LoadResult {
    long loadedRecords;
    long loadTimeMs;
    String versionKey;
}
```

### 6.8 VersionService — 版本管理

```java
package com.platform.tagquery.service;

public interface VersionService {

    /**
     * 为入库完成的数据创建版本
     * @param dataSourceId       数据源标识
     * @param integrationTaskId  集成任务 ID
     * @param recordCount        入库记录数
     * @return 版本信息
     */
    VersionDTO createVersion(String dataSourceId, Long integrationTaskId,
                             long recordCount);

    /**
     * 校验版本数据质量（重复率、空值率阈值检查）
     * @param versionId 版本 ID
     * @return 校验结果（PASSED / FAILED）
     */
    ValidationResult validateVersion(Long versionId);

    /**
     * 激活（切换生效）指定版本
     * 1. 校验 version 状态为 PASSED
     * 2. 更新 Redis active_version 指针
     * 3. 更新 DB active_status
     * 4. 记录 version_event_log
     * @param versionId 版本 ID
     * @param reason    操作原因
     */
    void activateVersion(Long versionId, String reason);

    /**
     * 回退到上一可用版本
     * 1. 找上一 ACTIVE/DEPRECATED 版本
     * 2. 更新 Redis active_version 指针
     * 3. 更新 DB active_status
     * 4. 记录 version_event_log
     * @param dataSourceId 数据源标识
     * @param reason       回退原因
     * @return 回退到的版本
     */
    VersionDTO rollbackVersion(String dataSourceId, String reason);

    /** 分页查询版本历史 */
    PageResult<VersionDTO> listVersions(String dataSourceId, int page,
                                         int size);

    /** 查询单个版本详情 */
    VersionDTO getVersion(Long versionId);

    /** 获取指定数据源的当前生效版本 */
    VersionDTO getActiveVersion(String dataSourceId);
}
```

### 6.9 LogService — 日志记录

```java
package com.platform.tagquery.service;

public interface LogService {

    /** 记录查询调用日志（异步写入，避免阻塞查询链路） */
    void logQuery(QueryLog log);

    /** 记录鉴权失败日志 */
    void logAuthFailure(AuthFailureLog log);

    /** 记录权限失败日志 */
    void logPermissionFailure(PermissionFailureLog log);

    /** 记录限流日志 */
    void logRateLimit(RateLimitLog log);

    /** 记录数据拉取日志 */
    void logPullTask(PullTaskLog log);

    /** 记录数据集成日志 */
    void logIntegration(IntegrationLog log);

    /** 记录版本生效日志 */
    void logVersionActivation(VersionEventLog log);

    /** 记录版本回退日志 */
    void logVersionRollback(VersionEventLog log);
}

// === 日志数据模型 ===
class QueryLog {
    String requestId;   String appKey;  String dataSourceId;
    int idCount;        int hitCount;   long costMs;
    String status;      String versionKey;  String sourceIp;
    LocalDateTime createdAt;
}
class AuthFailureLog {
    String appKey;  boolean signaturePassed;  boolean timestampPassed;
    String failureReason;  LocalDateTime requestTime;  String sourceIp;
}
class PermissionFailureLog {
    String appKey;  String dataSourceId;  boolean hasPermission;
    String failureReason;  LocalDateTime requestTime;
}
class RateLimitLog {
    String appKey;  String dimension;  int threshold;
    int currentValue;  LocalDateTime triggerTime;
}
class PullTaskLog {
    String customerName;  String s3Path;  String pullRule;
    String partition;  LocalDateTime startTime;  LocalDateTime endTime;
    String status;  String failureReason;
}
class IntegrationLog {
    String dataSourceId;  String versionKey;  long recordCount;
    LocalDateTime startTime;  LocalDateTime endTime;
    String status;  String failureReason;
}
class VersionEventLog {
    String dataSourceId;  String action;  String fromVersionKey;
    String toVersionKey;  String result;  String reason;
    LocalDateTime createdAt;
}
```

### 6.10 NotificationService — 邮件通知

```java
package com.platform.tagquery.service;

public interface NotificationService {

    /**
     * 发送【数据拉取失败告警】邮件
     */
    void sendPullFailureAlert(String customerName, String triggerType,
            String s3Path, String partition,
            LocalDateTime startTime, LocalDateTime endTime,
            String failureReason);

    /**
     * 发送【数据集成失败告警】邮件
     */
    void sendIntegrationFailureAlert(String customerName, String triggerType,
            String s3Path, String partition,
            LocalDateTime startTime, LocalDateTime endTime,
            String failureReason);

    /**
     * 发送【数据同步完成通知】邮件
     * 包含：任务概况、本次数据情况、版本结果、与上次数据对比
     */
    void sendTaskCompleteNotification(String customerName, String triggerType,
            String s3Path, String partition,
            LocalDateTime startTime, LocalDateTime endTime,
            int fileCount, String fileSize, long idCount,
            String versionKey, String versionStatus,
            String fileSizeChange, String idCountChange);
}
```

### 6.11 ErrorCode — 错误码枚举

```java
package com.platform.tagquery.exception;

public enum ErrorCode {

    // === 鉴权相关 (10001-10099) ===
    APP_KEY_INVALID      (10001, "调用身份无效，请确认 AppKey 是否正确或是否已启用"),
    SIGNATURE_INVALID    (10002, "签名校验失败，请检查签名生成规则"),
    TIMESTAMP_EXPIRED    (10003, "请求已过期，请重新发起请求"),
    DATA_SOURCE_UNAUTHORIZED(10004, "以下为无效数据源：{0}"),

    // === 参数校验 (10101-10199) ===
    IDS_EMPTY            (10101, "用户标识不能为空"),
    IDS_EXCEED_LIMIT     (10102, "单次查询 ID 数超过上限：{0}"),
    PARAM_FORMAT_INVALID (10103, "输入参数格式不符合接口要求"),
    DATA_SOURCE_INVALID  (10104, "数据源标识无效：{0}"),

    // === 限流 & 超时 (10201-10299) ===
    RATE_LIMITED         (10201, "当前任务配置已达到调用限制，请稍后重试"),
    QUERY_TIMEOUT        (10202, "查询超时，请稍后重试"),

    // === 系统异常 (10301-10399) ===
    REDIS_UNAVAILABLE    (10301, "实时查询服务暂不可用，请稍后重试"),
    SYSTEM_ERROR         (10302, "系统异常，请联系平台服务方排查"),

    // === 通用 ===
    SUCCESS              (0,     "success");

    private final int code;
    private final String template;

    ErrorCode(int code, String template) { ... }

    /** 格式化错误消息，{0} {1} 占位符替换 */
    public String format(Object... args) {
        return MessageFormat.format(template, args);
    }
}
```

### 6.12 DataParser — 文件格式解析

```java
package com.platform.tagquery.util;

public class DataParser {

    /** 数据文件分隔符 */
    static final String SEPARATOR = "|";

    /**
     * 解析单行数据
     * 格式: {userId}|{idType}|{tagCode1}|{tagCode2}|...
     * 第 1 段 = 用户标识（查询 Key）
     * 第 2 段 = 标识类型
     * 第 3 段及以后 = 标签 Code 列表
     *
     * @param line       源文件中的一行
     * @param lineNumber 行号（用于错误追踪）
     * @return 解析结果，格式不正确返回 null 并记录异常行号
     */
    public static ParsedRecord parseLine(String line, int lineNumber);

    /**
     * 验证单条记录的合法性
     * - userId 非空
     * - 至少有一个 tagCode
     * - tagCode 格式合法（非空、无特殊字符）
     * @param record 解析后的记录
     * @return true=合法
     */
    public static boolean validateRecord(ParsedRecord record);

    /**
     * MD5 加盐编码（用于匹配查询输入的 Key）
     * 输入：调用方传入的加密手机号 → 输出：与数据文件中一致的 Key
     * @param rawId 原始标识
     * @param salt  盐值（从配置读取）
     * @return MD5(rawId + salt)
     */
    public static String encodeId(String rawId, String salt);
}
```

### 6.13 PullTaskScheduler — 定时拉取调度

```java
package com.platform.tagquery.scheduler;

@Component
public class PullTaskScheduler {

    /**
     * 每分钟扫描一次，检查是否有到达拉取时间的配置
     * 使用 Redis 分布式锁（SETNX + TTL）防止多实例重复执行
     */
    @Scheduled(cron = "0 * * * * ?")
    public void scanAndTrigger() {
        // 1. 尝试获取分布式锁: SET lock:pull_scheduler {instanceId} NX EX 55
        // 2. 获取锁失败 → 跳过（其他实例正在执行）
        // 3. pullConfigService.getDuePullConfigs() 获取到期的配置
        // 4. 逐个调用 pullTaskService.triggerScheduledPull(config)
        // 5. 释放锁（通过 TTL 自动过期）
    }
}
```

### 6.14 Middleware — 拦截器链路

```java
package com.platform.tagquery.middleware;

/**
 * 拦截器执行顺序（按注册顺序）:
 * 1. RequestIdInterceptor   → 生成并设置 requestId
 * 2. AuthInterceptor        → AppKey 鉴权 + 签名校验 + 时间戳 + 数据源授权
 * 3. RateLimitInterceptor   → QPS 限流
 * 4. [进入 Controller]
 */

@Component
public class AuthInterceptor implements HandlerInterceptor {

    @Override
    public boolean preHandle(HttpServletRequest request,
                             HttpServletResponse response, Object handler) {
        // 1. 从请求中提取 appKey, signature, timestamp, dataSourceId, ids
        //    - appKey / signature / timestamp 从 Header: X-AppKey / X-Signature / X-Timestamp
        //    - dataSourceId / ids 从 Body（需要先缓存 Body）
        // 2. authService.validateAppKey(appKey)
        // 3. authService.validateTimestamp(timestamp, 300)
        // 4. authService.checkDataSourceAuth(appKey, dataSourceId)
        // 5. authService.validateSignature(appKey, timestamp, ids, dataSourceId, signature, secret)
        // 任一失败 → 抛出 BizException → GlobalExceptionHandler 统一处理
        return true;
    }
}

@Component
public class RateLimitInterceptor implements HandlerInterceptor {

    @Override
    public boolean preHandle(HttpServletRequest request,
                             HttpServletResponse response, Object handler) {
        // 1. 提取 appKey, dataSourceId
        // 2. rateLimitService.tryAcquire(appKey, dataSourceId)
        // 3. 失败抛 BizException(RATE_LIMITED)
        return true;
    }
}
```

---

## 七、2 周逐日开发计划（10 工作日）

### Day 1：项目初始化 & 数据库 & 接口设计

| # | 任务 | 预计耗时 | 核心产出 |
|---|------|----------|----------|
| 1.1 | 初始化 Maven 工程：Spring Boot + MyBatis-Plus + Redis + Flyway + Prometheus | 1h | 项目可编译启动 |
| 1.2 | 编写 8 张表的 Flyway DDL `V1__init_schema.sql` | 1.5h | 表结构就绪 |
| 1.3 | 创建 `ErrorCode` 枚举（14 种错误码 + format 方法） | 30min | `ErrorCode.java` |
| 1.4 | 创建 `model/entity/` 下 8 个 DB 实体类 | 1h | 实体类 |
| 1.5 | 创建 `model/dto/` 下 DTO：`TagQueryRequest`, `TagQueryResponse`, `CreatePullConfigRequest` 等 | 1h | DTO 定义 |
| 1.6 | 创建 `GlobalExceptionHandler` + `BizException` | 30min | 异常统一处理 |
| 1.7 | 搭建 `docker-compose.yml`（MySQL 8.0 + Redis 7） | 30min | 本地可启动 |
| 1.8 | 编写 `POST /api/v1/tag/query` 的 OpenAPI 3.0 spec | 1h | `docs/api-spec.yaml` |

**Day 1 验收标准**：`mvn clean package` 成功，数据库表建好，空 `TagQueryController` 返回 Mock JSON。

---

### Day 2：鉴权中间件 + AppKey 管理 API

| # | 任务 | 预计耗时 | 核心产出 |
|---|------|----------|----------|
| 2.1 | 实现 `AuthService` 全量方法 | 2h | 鉴权逻辑层 |
| 2.2 | 实现 `SignatureUtil`（HMAC-SHA256 签名生成和校验） | 1h | 签名工具 |
| 2.3 | 实现 `AuthInterceptor`（注册到拦截器链） | 1h | 鉴权拦截器 |
| 2.4 | 实现 `AppKeyController` 6 个管理 API | 1.5h | AppKey CRUD |
| 2.5 | 实现 AppKey ↔ DataSource 授权映射逻辑 | 30min | 授权关联 |
| 2.6 | 单元测试 `AuthServiceTest`：覆盖 4 种鉴权失败场景 | 1h | 测试通过 |

**Day 2 验收标准**：不带 AppKey → 返回 code=10001；错误签名 → 返回 code=10002；过期时间戳 → 返回 code=10003；未授权数据源 → 返回 code=10004。

---

### Day 3：限流 + 核心查询逻辑 + Redis 读写

| # | 任务 | 预计耗时 | 核心产出 |
|---|------|----------|----------|
| 3.1 | 实现 `RateLimitService`（Redis 滑动窗口） | 1.5h | 限流逻辑 |
| 3.2 | 实现 `RateLimitInterceptor` + 注册到拦截器链 | 30min | 限流拦截器 |
| 3.3 | 实现 `TagRedisRepository` 全量方法（batchGetTags / setActiveVersion / getActiveVersion / ping） | 2h | Redis 读写层 |
| 3.4 | 实现 `TagQueryService.queryTags()` + `batchReadFromRedis()` + `getActiveVersion()` | 2h | 核心查询链 |
| 3.5 | 实现 `TagQueryController` 查询 API | 30min | API 端点 |
| 3.6 | Redis 预置 Mock 数据，端到端验证：鉴权→限流→查 Redis→组装返回 | 1h | 全链路可查 |
| 3.7 | 单元测试 `TagQueryServiceTest` | 30min | 覆盖率 |

**Day 3 验收标准**：`curl -X POST /api/v1/tag/query ...` 返回 Mock 数据中的标签列表，命中/未命中均正确处理。

---

### Day 4：查询完善 + 全量错误码 + 日志记录

| # | 任务 | 预计耗时 | 核心产出 |
|---|------|----------|----------|
| 4.1 | 参数校验：ids 为空/超限/dataSourceId 无效 → 对应错误码 | 30min | 参数校验完整 |
| 4.2 | 空结果处理：ID 无标签 → 返回 `tags: []`，HTTP 200，stats.hitRate=0 | 30min | 空结果正确 |
| 4.3 | 查询超时控制：给 Redis Pipeline 设置超时 → 超时抛 QUERY_TIMEOUT | 30min | 超时降级 |
| 4.4 | Redis 不可用降级：ping 失败 → 返回 REDIS_UNAVAILABLE | 30min | 降级处理 |
| 4.5 | 全量 14 种错误码触发场景验证，补全遗漏的 code | 1h | 14 种全覆盖 |
| 4.6 | 实现 `RequestIdInterceptor` + `RequestIdGenerator` | 30min | requestId 透传 |
| 4.7 | 实现 `LogService` 接口 + `logQuery()` MySQL 写入（异步 @Async） | 1h | 查询日志 |
| 4.8 | 实现鉴权失败、权限失败、限流日志 + 在各拦截器中埋点 | 1.5h | 3 类日志自动记录 |
| 4.9 | 集成测试：完整链路 + 各类异常场景 | 1h | 测试通过 |

**Day 4 验收标准**：14 种错误码均能正确触发并返回中文文案；查询/鉴权/限流日志正确写入 MySQL。

---

### Day 5：S3 拉取配置 + 拉取执行

| # | 任务 | 预计耗时 | 核心产出 |
|---|------|----------|----------|
| 5.1 | 实现 `S3Client`：listObjects / downloadFile / checkExists | 1.5h | S3 操作封装 |
| 5.2 | 实现 `PullConfigService` 全量方法 | 1.5h | 配置 CRUD |
| 5.3 | 实现 `PullConfigController`（5 个管理 API） | 1h | 配置管理 API |
| 5.4 | 实现 `PullTaskService.triggerManualPull()`：创建任务 → 解析分区 → S3 下载 → 记录结果 | 1.5h | 手动触发链路 |
| 5.5 | 实现 `resolvePartition()`：LATEST = 找最大分区；CURRENT_MONTH = yyyy-MM | 30min | 分区解析 |
| 5.6 | 实现 `PullTaskController`（3 个 API） | 30min | 任务管理 API |
| 5.7 | 实现拉取任务日志 `logPullTask()` | 30min | 拉取日志 |
| 5.8 | 用测试 S3 环境端到端验证：配置 → 手动触发 → 文件下载到本地 | 1h | 拉取链路通 |

**Day 5 验收标准**：配置一个 S3 地址，手动触发拉取 → 文件成功下载到本地临时目录 → `pull_task` 表状态正确。

---

### Day 6：数据解析 & 入库（ETL 完整链路）

| # | 任务 | 预计耗时 | 核心产出 |
|---|------|----------|----------|
| 6.1 | 实现 `DataParser.parseLine()` + `validateRecord()` | 1h | 行解析器 |
| 6.2 | 实现 `parseDataFile()` 流式迭代器（`BufferedReader` 逐行读取，避免大文件 OOM） | 1h | 流式读取 |
| 6.3 | 实现 `DataIntegrationService.validateData()`：统计 5 项指标 | 1h | 校验逻辑 |
| 6.4 | 实现 `DataIntegrationService.loadToRedis()`：Pipeline 分批写入 | 2h | Redis 入库 |
| 6.5 | 实现 `runIntegration()`：解析 → 校验 → 入库 完整串联 | 1h | ETL 调度 |
| 6.6 | 拉取成功后自动触发集成（`triggerManualPull()` 结尾调 `runIntegration()`） | 1h | 自动化串联 |
| 6.7 | 实现集成任务日志 `logIntegration()` | 30min | 集成日志 |
| 6.8 | 端到端验证：S3 测试文件 → 下载 → 解析 → 校验 → 灌入 Redis → 查询可读 | 1h | 全链路 |

**Day 6 验收标准**：S3 上的测试标签文件（100 万行模拟数据），从拉取到可查询全链路自动化，无人工干预。

---

### Day 7：版本管理完整闭环

| # | 任务 | 预计耗时 | 核心产出 |
|---|------|----------|----------|
| 7.1 | 实现 `VersionService.createVersion()`：集成成功后 → 生成 `version_key` → 写入 DB | 1h | 版本创建 |
| 7.2 | 实现 `VersionService.validateVersion()`：校验 duplicateRate / nullRate 是否超阈值 | 1h | 版本校验 |
| 7.3 | 实现 `VersionService.activateVersion()`：更新 DB + 更新 Redis active_version 指针 | 1h | 版本切换 |
| 7.4 | 实现 `VersionService.rollbackVersion()`：找到上一可用版本 → 切回 | 1h | 版本回退 |
| 7.5 | 实现 `VersionController`（3 个管理 API） | 30min | 版本管理 API |
| 7.6 | 串联 ETL → 自动创建版本 + 自动校验 | 1.5h | 自动化 |
| 7.7 | 实现版本生效/回退日志 | 30min | 版本日志 |
| 7.8 | 异常场景覆盖：校验失败不生成可生效版本 / 回退失败保持现状 | 1h | 异常路径 |

**Day 7 验收标准**：入库完成 → 自动生成版本 → 可在管理 API 手动激活/回退，同一查询请求只命中同一版本，异常路径正确。

---

### Day 8：定时调度 + 邮件通知 + Prometheus 监控

| # | 任务 | 预计耗时 | 核心产出 |
|---|------|----------|----------|
| 8.1 | 实现 `PullTaskScheduler`：`@Scheduled(cron="0 * * * * ?")` + Redis 分布式锁 | 1h | 定时拉取 |
| 8.2 | 实现 `EmailClient`（封装 JavaMailSender，配置 SMTP） | 30min | 邮件发送 |
| 8.3 | 实现 `NotificationService` 三类邮件模板（Thymeleaf HTML） | 1.5h | 邮件通知 |
| 8.4 | 在拉取失败/集成失败/任务完成各节点触发对应邮件 | 1h | 通知链路 |
| 8.5 | Prometheus 8 项指标埋点：使用 Micrometer 或手动 Counter/Gauge | 2h | 全量指标 |
| 8.6 | Grafana 大盘：4 张看板 JSON（查询概览、数据链路、版本状态、调用方维度） | 1h | 监控看板 |
| 8.7 | 健康检查 `GET /api/v1/health`：检查 Redis / MySQL / S3 连通性 | 30min | 健康探针 |

**Day 8 验收标准**：定时任务自动触发拉取 → 邮件通知正常发送 → `localhost:8080/actuator/prometheus` 可抓取到 8 项指标。

---

### Day 9：联调 & 边界场景修复 & 数据隔离验证

| # | 任务 | 预计耗时 | 核心产出 |
|---|------|----------|----------|
| 9.1 | 全链路联调：定时拉取 → 集成 → 版本自动生效 → 查询可读 | 2h | 闭环验证 |
| 9.2 | 16 个异常场景逐一回归测试并记录结果 | 2h | 异常矩阵文档 |
| 9.3 | 数据隔离验证：客户 A 数据源 → AppKey B 无权查询 → 返回 code=10004 | 1h | 多租户隔离 |
| 9.4 | 并发测试：多 AppKey 同时高频查询 → 限流正常、互不影响 | 30min | 隔离验证 |
| 9.5 | 边界场景修复 + 全量回归测试 | 2h | Bug 清零 |

**16 个异常场景清单**：

| # | 场景 | 预期错误码 |
|---|------|------------|
| 1 | AppKey 不存在 | 10001 |
| 2 | AppKey 已停用 | 10001 |
| 3 | 签名校验失败 | 10002 |
| 4 | 时间戳过期（过去 10 分钟） | 10003 |
| 5 | dataSourceId 未授权给该 AppKey | 10004 |
| 6 | dataSourceId 格式无效 | 10104 |
| 7 | ids 为空数组 | 10101 |
| 8 | ids 超过批量上限 | 10102 |
| 9 | 请求 JSON 格式错误 | 10103 |
| 10 | 触发 QPS 限流 | 10201 |
| 11 | 查询超时（Redis 慢查询） | 10202 |
| 12 | Redis 完全不可用 | 10301 |
| 13 | 系统未知异常 | 10302 |
| 14 | S3 地址不可达 | 拉取失败告警邮件 |
| 15 | 数据文件格式错误 | 集成失败告警邮件 |
| 16 | 数据文件重复率超阈值 | 校验失败，不生成版本 |

**Day 9 验收标准**：16 个异常场景全部回归通过，多租户数据隔离验证无问题。

---

### Day 10：压测 & 文档 & 发布准备

| # | 任务 | 预计耗时 | 核心产出 |
|---|------|----------|----------|
| 10.1 | 编写 Locust 压测脚本（模拟真实查询流量，含鉴权 Header） | 1.5h | `locustfile.py` |
| 10.2 | QPS 1000 压测，记录 P99 / P95 / avg RT / CPU / 内存 / Redis 连接数 / 错误率 | 1.5h | 压测报告 |
| 10.3 | 不同批量上限测试：100 / 500 / 1000 / 2000 / 5000，记录 RT 曲线 | 1h | 批量上限建议 |
| 10.4 | 十亿级数据容量评估：脚本生成模拟数据 → 灌入 → 记录 Redis 内存 / 查询延迟 | 1h | 容量评估报告 |
| 10.5 | 编写部署文档 `deploy-guide.md`：环境变量、S3 配置、Redis 集群要求、启动步骤 | 1h | 部署文档 |
| 10.6 | 编写运维手册 `ops-guide.md`：常见故障 SOP（Redis 宕机、拉取失败、版本回退） | 1h | 运维文档 |
| 10.7 | 最终代码 Review + 格式化 + 补充注释 + 清理 TODO | 1h | 代码就绪 |

**Day 10 验收标准**：压测报告输出，P99 ≤ 1000ms；部署/运维文档可交付；服务可达上线标准。

---

## 八、依赖确认清单（Day 1 前必须完成）

| # | 待确认事项 | 负责人 | 确认方式 |
|---|------------|--------|----------|
| 1 | 手机号加密方案：MD5 加盐的**具体盐值**和**编码方式**确认 | 数据提供方 | 邮件/会议 |
| 2 | 技术栈最终确认：Java/Go？Spring Boot 版本？Redis 单机还是 Cluster？ | 技术负责人 | 内部对齐 |
| 3 | S3 测试环境：提供测试 Bucket + AccessKey + SecretKey | 平台方/客户 | 申请资源 |
| 4 | SMTP 邮件服务配置 | 运维 | 申请账号 |
| 5 | Prometheus + Grafana 基础设施就绪 | 运维 | 确认可用 |
| 6 | 法务合规：实时 API 场景是否需要额外审批 | PM | 发起流程 |
| 7 | 压测环境资源：至少 4C8G × 2 实例 + Redis Cluster | 运维 | 申请资源 |

---

## 九、风险与缓解矩阵

| 风险 | 概率 | 影响 | 影响 Day | 缓解措施 |
|------|------|------|----------|----------|
| 十亿级 Redis 内存不足 | 中 | 高 | D10 | 1 条记录约 200B × 10亿 ≈ 200GB；提前评估，必要时 Redis on SSD / Pika / 分片 |
| S3 跨网络拉取慢 | 低 | 中 | D5 | 同区域部署，流式下载，设合理超时 |
| MD5 加盐方案未确认 | 中 | 高 | D1 | Day 1 前必须确认，否则阻塞整个查询链路 |
| 压测 QPS 不达 1000 | 中 | 高 | D10 | 预留优化空间：Connection Pool 调优、本地缓存、水平扩展 |
| 批量上限不理想 | 低 | 中 | D10 | 压测后给出推荐上限，与调用方协商 |
| 合规审批卡住 | 中 | 高 | D1 | 与 PM 同步推进，不阻塞开发但可能阻塞上线 |

---

## 十、交付标准（Definition of Done）

- [ ] ✅ `POST /api/v1/tag/query` AppKey 鉴权 + 数据源授权 + QPS 限流 + 超时控制完整
- [ ] ✅ 全部 14 种错误码可正确触发并返回中文文案
- [ ] ✅ S3 拉取 → 解析 → 校验 → 灌入 Redis 全链路自动串联
- [ ] ✅ 版本生成 → 校验 → 生效 → 回退完整闭环
- [ ] ✅ 9 类日志写入 MySQL，8 项 Prometheus 指标可抓取
- [ ] ✅ 3 类邮件通知模板可用
- [ ] ✅ QPS 1000 压测 P99 ≤ 1000ms
- [ ] ✅ 十亿级容量评估报告完成
- [ ] ✅ 16 个异常场景回归通过
- [ ] ✅ 部署文档 + 运维手册交付
- [ ] ✅ 单元测试覆盖率 ≥ 70%

---

> **制定日期**：2026-07-25  
> **计划版本**：v2.0（详细版）  
> **适用人员**：独立开发  
> **下次评审**：Day 5 中期 Checkpoint / Day 10 终期交付评审
